# .goreleaser.yaml
# Production-ready GoReleaser configuration for tradingview-calendar-go
version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: tvcalendar
    main: ./cmd/tvcalendar/main.go
    binary: tvcalendar
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

  - id: tv-notifier
    main: ./cmd/tv-notifier/main.go
    binary: tv-notifier
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    files:
      - LICENSE
      - README.md

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request
      - Merge branch
